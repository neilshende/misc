#Using su to become root,
#command cp ~dstanton/misc/dstanton.sudoers /etc/sudoers.d/dstanton
#command chmod 440                          /etc/sudoers.d/dstanton

#I also tried:	chgrp dstanton /etc/sudoers.d/dstanton
#This succeeded, but dstanton could still not read that file,
#probably because /etc/sudoers.d is accessible only to root.

## Allow vshende to run any commands anywhere 
vshende	ALL=(ALL) 	NOPASSWD:ALL
